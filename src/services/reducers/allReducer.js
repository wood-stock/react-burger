const initialState = {
  burgerIngredients: [],
  addIngredients: {},
  selectedIngredient: {},
  order: {
    id: 'I here',
  },
};
export const allReducer = (state = initialState, action) => {
  return state;
};
